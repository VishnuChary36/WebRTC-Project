{
  "name": "webrtc-frontend",
  "version": "1.0.0",
  "description": "Frontend for WebRTC VLM Object Detection",
  "main": "src/app.js",
  "scripts": {
    "build": "if not exist dist mkdir dist && copy public\\index.html dist\\ && copy src\\app.js dist\\",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
